#include <iostream>
int main(){
    /*
    vetctor<vector2<int>> primePairs
    int val = 4;
    while(eveNubers.size()<n){
    int p = 2;
    bool found = false;
    while(!found){
    if(is_prime(p)&&is-primw(val-p)){
    found = true;
    vetctor<int> pair = {p, val-p};
    primePairs.push_back(pair);
        }
    }
        p++;
    }
        val =val+2
}
        return primePairs;
    */
}